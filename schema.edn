;; schema.edn — Datomic/Datascript-compatible schema for this repo's EDN
;; tx-data files (accumulated by hand from manifest/edn-datomize.cljs's
;; classify logic in the com-junkawasaki superproject; no Datomic-only
;; keys like :db.install/_attribute).
;;
;; Source: resources/signal_integrity/constants.edn (all keys already
;; idiomatically namespaced by source subsystem: :tline/* :crosstalk/*
;; :eye/* -- kept as-is, not re-prefixed).

[#:db{:ident :crosstalk/backward-coefficient,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :crosstalk/driver-amplitude-mv,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :crosstalk/driver-source-impedance-ohm,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :crosstalk/forward-coefficient,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :crosstalk/saturation-floor-mm,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :crosstalk/spacing-decay-mm,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :eye/center-window-end,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :eye/center-window-start,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :eye/default-q-factor,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :eye/jitter-pp-multiplier,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :eye/lcg-increment,
      :valueType :db.type/long,
      :cardinality :db.cardinality/one}
 #:db{:ident :eye/lcg-multiplier,
      :valueType :db.type/long,
      :cardinality :db.cardinality/one}
 #:db{:ident :eye/lcg-seed,
      :valueType :db.type/long,
      :cardinality :db.cardinality/one}
 #:db{:ident :eye/samples-per-bit,
      :valueType :db.type/long,
      :cardinality :db.cardinality/one}
 #:db{:ident :eye/sigma-multiplier,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :eye/xorshift32-seed,
      :valueType :db.type/long,
      :cardinality :db.cardinality/one}
 #:db{:ident :tline/c-mm-per-ps,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :tline/coplanar-loss-base,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :tline/microstrip-loss-base,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :tline/stripline-loss-base,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}
 #:db{:ident :tline/trace-thickness-mm,
      :valueType :db.type/double,
      :cardinality :db.cardinality/one}]
